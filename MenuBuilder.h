#pragma once

#include "CncTools.h"

class MenuBuilder
{
public:
    /********** CONSTRUCTOR **********/

    /*
     * Constructs the MenuBuilder object.
     */
	MenuBuilder();

    /********** DESTRUCTOR **********/

    /*
     * Destructs the MenuBuilder object.
     */
    ~MenuBuilder();
};
